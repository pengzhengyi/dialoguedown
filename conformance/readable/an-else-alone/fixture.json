{
  "$schema": "https://pengzhengyi.github.io/dialoguedown/schema/fixture-0.schema.json",
  "name": "an else with no gated arm is refused",
  "playbook": "playbook.json",
  "verdict": "refuse",
  "because": "the gated arm is removed, leaving only the else; an else falls back from the gated arms, so with none the branch is not a condition"
}
