{
  "$schema": "https://pengzhengyi.github.io/dialoguedown/schema/fixture-0.schema.json",
  "name": "a second else arm is refused",
  "playbook": "playbook.json",
  "verdict": "refuse",
  "because": "a second conditionless arm is added, but an else is the fallback and there can be only one"
}
