{
  "$schema": "https://pengzhengyi.github.io/dialoguedown/schema/fixture-0.schema.json",
  "name": "unknown requires is refused",
  "playbook": "playbook.json",
  "verdict": "refuse",
  "because": "requires 'detour', and a runtime that cannot detour would tell a different story rather than fail"
}
